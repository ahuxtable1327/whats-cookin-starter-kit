class RecipeRepository {
  constructor(recipes) {
    this.recipes = recipes;
    console.log(this.recipes)
  }
  filterByTag(tag) {

  }
  // return a filtered array of recipes based on the tags
  // my tags are an array within the recipe object
  // return any recipe that has a tag that includes the search
  // 
}

export default RecipeRepository;
